"""dnsrebindjack — a container-only educational demo of DNS rebinding (a TOCTOU flaw in
hostname resolution) defeating an SSRF egress control.

Everything in this package is wholly fictional and runs only inside the demo's own internal
container network. No real host, resolver, or service is ever contacted, and no real DNS is used.
"""
