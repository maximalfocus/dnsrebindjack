"""The webhook-integrations application: a secure default plus (in later slices) the
intentionally-vulnerable and half-fixed variants, all sharing one auth contract, request/response
shape, probe lifecycle, and audit event. Only the fetch engine differs between variants.
"""
